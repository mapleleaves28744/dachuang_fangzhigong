# 联调验收单

项目：FZG 智能学习伴侣

验收目标：确认后端能力已在前端完整展示，避免“后端有、前端看不见”。

## 结论

本次联调已通过。后端接口稳定可用，前端已展示检索模式、结果卡片、分数构成、响应时间和来源信息，前后端功能对齐。

## 环境

| 项目 | 结果 |
|---|---|
| 后端健康检查 | 通过 |
| 前端页面加载 | 通过 |
| 知识库检索接口 | 通过 |
| E2E 回归 | 19/19 通过 |

## 接口验收

| 接口 | 验收点 | 结果 |
|---|---|---|
| /health | 服务状态正常 | 通过 |
| /api/agent/kb/search | 支持 search_mode，返回 hits / query_time_ms / retrieval_mode | 通过 |
| /api/ask | 正常问答返回 | 通过 |
| /api/content/ingest_async | 异步入库与任务轮询正常 | 通过 |
| /api/knowledge_graph/* | 图谱查询与路径接口正常 | 通过 |

## 前端展示点

| 展示项 | 页面表现 | 结果 |
|---|---|---|
| 检索模式切换 | 混合 / 向量 / 词法 | 通过 |
| 结果卡片 | 结果以卡片形式展示 | 通过 |
| 分数展示 | 显示综合分、向量分、词法分 | 通过 |
| 响应时间 | 显示 query_time_ms | 通过 |
| 来源信息 | 显示 channel / 来源类型 | 通过 |

## 通过证据

| 证据文件 | 说明 |
|---|---|
| [docs/e2e_summary.json](docs/e2e_summary.json) | E2E 回归结果，19/19 通过 |
| [frontend_upgrade_report.json](frontend_upgrade_report.json) | 前端对齐校验结果 |
| [frontend/chat.html](frontend/chat.html) | 页面包含知识库检索入口与模式选择器 |
| [frontend/assets/js/pages/chat-page.js](frontend/assets/js/pages/chat-page.js) | 前端已接入 search_mode 与 HTML 结果渲染 |
| [backend/app/api/agent_routes.py](backend/app/api/agent_routes.py) | 后端透传 search_mode 到检索服务 |

## 复现步骤

1. 打开前端页面。
2. 输入查询词，例如“一次函数”。
3. 切换检索模式并点击检索。
4. 观察返回结果是否显示卡片、分数、响应时间和来源。
5. 复核 /health 与 E2E 结果文件。

## 备注

当前无需再改代码。若后续需要更强的结果多样性，可继续在检索排序侧增加重排策略，但不影响本次验收结论。